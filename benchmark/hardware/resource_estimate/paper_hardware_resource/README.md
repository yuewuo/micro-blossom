# Hardware resource under different configurations

X axis: code distance
Y axis: LUT usage, register usage

```sh
python3 prepare.py  # takes several days...
python3 run.py  # takes hours if first run, generate the txt data
python3 plot.py
```
